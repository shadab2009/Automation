import { test, expect } from '@playwright/test'; test('simulator page is usable', async ({page})=>{await page.goto('/'); await expect(page.getByText('Message simulator')).toBeVisible(); await page.getByPlaceholder('+1 555 0100').fill('test-001'); await page.getByPlaceholder('Write a test message...').fill('Hello Shadab'); await page.getByRole('button',{name:'Simulate incoming message'}).click(); await expect(page.getByText('Automatic reply generated')).toBeVisible();});
